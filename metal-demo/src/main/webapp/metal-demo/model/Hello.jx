/**
 * @resource Hello
 * @imports Foo
 * @imports Bar
 */

//@static
function parseModel(source) {
	Bar.loadResource(source.model.bar);
}

//@private
function initObject(object, model) {
	object.set("foo", new Foo(model.foo));
	object.set("bar", Bar.findResource(model.bar));
}
