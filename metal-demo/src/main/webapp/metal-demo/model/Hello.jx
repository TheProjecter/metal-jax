/**
 * @resource Hello
 * @imports Foo
 * @imports Bar
 */

//@private
function parseModel(model) {
	this.set("foo", new Foo(model.foo));
	Bar.load(model.bar);
}

//@private
function initObject(model) {
	this.set("bar", Bar.find(model.bar));
}
