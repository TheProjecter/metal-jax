/**
 * @resource Hello
 * @imports Foo
 * @imports Bar
 */

//@private
function parseModel(model) {
	this.set("foo", new Foo(model.foo));
	Bar.loadResource(model.bar);
}

//@private
function initObject(model) {
	this.set("bar", Bar.findResource(model.bar));
}
