/**
 * @resource
 * @setting resource.url
 * @setting resource.object
 * 
 * @copyright Jay Tang 2012. All rights reserved.
 */

//@protected
function initClass(scope) {
	if (scope.$class.$("resource.url")) {
		scope.$imports[$class.getName()] = $class.getName();
		scope.$requires[scope.$class.getName()] = $class.getName();
		log("info", "initClass scope for: " + scope.$class.getName());
	}
}

//@static
function checkResource() {
	var name = this.$("resource.object");
	if (name) {
		return typeof window[name] != "undefined";
	}
}
