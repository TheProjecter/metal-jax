/**
 * @resource Messages
 * 
 * @setting resource.module=messages
 * @setting resource.type=properties
 */

//@private
function initResource(source) {
	for (var name in source.model) {
		if (!(name in this.$setting)) {
			this.$setting[name] = source.model[name];
		}
	}
}
