modus.init.bootContext=modus init with bootContext: ${bootModule}:${bootContext}
modus.init.baseContext=modus init with baseContext: ${baseModule}:${baseContext}
modus.init.baseContext.fail=modus init with baseContext failed: ${baseModule}:${baseContext}
modus.init.baseModule.default=modus init with default baseModule: ${baseModule}
modus.init.baseResource=modus init with baseResource: ${baseResource|<empty>}:${baseClass|<empty>}
modus.init.baseResource.fail=modus init with baseResource failed: ${0}
modus.resource.cycle=resource in reference cycle: ${0}
