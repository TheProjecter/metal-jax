/**
 * @model model.Hello
 * @imports Foo
 * @imports Bar
 */

//@static
function parseModel(model) {
	this.foo = new Foo(model.foo);
	Bar.load(model.bar);
}

//@static
function initModel(model) {
	this.bar = Bar.find(model.bar);
}
