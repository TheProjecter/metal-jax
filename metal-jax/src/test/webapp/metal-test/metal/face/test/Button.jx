/**
 * @face Button
 */

//@private
function initObject(node) {
	this.nodes.button;
	this.nodes.label.innerHTML += " - Button.jx";
	this.bind(hello, this.nodes.button, "click");
}

//@private
function hello() {
	alert(this.nodes.label.innerHTML);
}
