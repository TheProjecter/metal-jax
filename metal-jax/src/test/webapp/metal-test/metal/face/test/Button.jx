/**
 * @face Button
 */

//@private
function initObject(object, node) {
	object.nodes.button;
	object.nodes.label.innerHTML += " - Button.jx";
	object.bind(hello, object.nodes.button, "click");
}

//@private
function hello() {
	alert(this.nodes.label.innerHTML);
}
