/**
 * @context face
 * @imports metal.face.base Base
 * @requires ${baseClass|metal.jax.Face} ${base}
 * @source base=${baseURL} module=${baseModule} includes=[.]test[.]
 * @source base=${baseURL} module=${baseModule} includes=Test$
 */

//@private
function initClass() {
	Base.initClassScope(this);
}

//@private
function initContext() {
	Base.initContextScope(this);
}
