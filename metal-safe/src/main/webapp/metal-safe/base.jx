/**
 * @context base
 * @imports metal.face.base Base
 * @requires ${baseClass|metal.jax.Face} ${base}
 */

//@private
function initResource(scope) {
	Base.initClassScope(scope);
}

//@private
function initContext(scope) {
	Base.initContextScope(scope);
}
