/**
 * @context
 * @imports metal.face.Boot
 * @requires ${baseClass|metal.jax.Face} ${baseResource}
 */

//@private
function initResource(scope) {
	Boot.initClassScope(scope);
}

//@private
function initContext(scope) {
	Boot.initContextScope(scope);
}
